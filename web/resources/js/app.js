// Digital product e-commerce store
// ---------------------------------
// The customer site and admin panel use inline <script> blocks embedded
// directly in their Blade layouts, so no heavy application bundle is required.
//
// This file is kept intentionally minimal so that the `npm run build`
// (Vite) pipeline succeeds. It provides lazy-loading support for any future
// modules without pulling in a full framework.

// Example: dynamically import a component only when it is actually needed.
// const loadCarousel = () => import('./carousel');
